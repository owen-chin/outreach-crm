name: Send Due Drafts

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call /internal/send-due-drafts
        run: |
          curl -sS -f --retry 3 --retry-delay 5 --retry-all-errors \
            -X POST "${{ secrets.BACKEND_URL }}/internal/send-due-drafts" \
            -H "x-internal-secret: ${{ secrets.INTERNAL_API_SECRET }}"
